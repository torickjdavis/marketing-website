{
  "name": "marketing-website-calm-mind",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "postinstall": "mkdir src src/css src/scss src/js src/assets && touch src/index.html",
    "start": "node_modules/.bin/parcel $npm_package_main --port $PORT",
    "build": "node_modules/.bin/parcel build $npm_package_main -no-minify",
    "sassy": "node_modules/.bin/sass --watch src/scss:src/css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.9.7",
    "sass": "^1.14.0"
  },
  "dependencies": {
    "node-sass": "^4.9.3"
  }
}
